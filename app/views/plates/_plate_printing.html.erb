<%= section do %>
  <h2>Print plate label</h2>
  <%= individual_barcode_printing_form(
        @presenter.labware.barcode,
        :redirection_url => illumina_c_plate_path(@presenter.labware),
        :stock_barcode   => "#{@presenter.labware.stock_plate.try(:barcode).try(:prefix)}#{@presenter.labware.stock_plate.try(:barcode).try(:number)}",
        :text            => "#{@presenter.label_text}"
  ) %>
<% end %>

